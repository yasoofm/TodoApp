<div>
    @Item.Id @Item.Description (@(Item.IsCompleted ? "Done" : "In progress"))
</div>

@code {
    [Parameter] public Todo Item { get; set; }

    protected override void OnInitialized()
    {
        base.OnInitialized();
    }
}
